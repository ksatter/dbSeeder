// #!/usr/bin/env node
var app = require('./../app');
var path = require('path')

var cwd = process.cwd();
console.log(cwd) 

// var file = path.join(cwd, process.argv[2]);
// conaolw.log(file);

// app.run(file)